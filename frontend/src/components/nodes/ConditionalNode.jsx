import React from 'react';
import DynamicNode from './DynamicNode';

const ConditionalNode = (props) => {
  // You can customize the ConditionalNode here if needed
  return <DynamicNode {...props} />;
};

export default ConditionalNode;
